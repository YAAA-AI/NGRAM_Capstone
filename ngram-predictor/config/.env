# N-gram Text Predictor Configuration
# ------------------------------------

# Smoothing method: "none" (MLE backoff without unigram, default)
# or "mle-backoff" (MLE backoff with unigram fallback)
SMOOTHING=none
