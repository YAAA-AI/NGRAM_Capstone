# N-gram Text Predictor Configuration
# ------------------------------------

# Smoothing method: "mle-backoff" (MLE backoff with unigram fallback, default)
# or "katz" (Katz backoff with Good-Turing discounting)
SMOOTHING=mle-backoff
